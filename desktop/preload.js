// ============================================================
// REWARDLY — Electron (preload)
// Aucune API Node exposée au contenu web : l'application est chargée
// depuis l'URL distante (privé). Isolation maximale (contextIsolation+).
// ============================================================